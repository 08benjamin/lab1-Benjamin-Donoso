#lang racket
;archivo contenedor de los tda

;tda
(define (archivo nombre)
  (if (string? nombre)
      (list nombre)
  #f
  ) 
)


;buen tda de zonas UwU.
(define (zonas)
  (define workplace (list))
  (define index (list))
  (define local_repository (list))
  (define remote_repository (list))
  (list workplace index local_repository remote_repository)
)



(provide zonas)
(provide archivo)